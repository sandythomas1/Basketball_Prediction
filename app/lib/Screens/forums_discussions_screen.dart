import 'package:flutter/material.dart';

class ForumsDiscussionsScreen extends StatelessWidget {
  const ForumsDiscussionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Forums')),
      body: const Center(child: Text('Forums')),
    );
  }
}